{
  "name": "anti-fraud",
  "version": "1.0.0",
  "description": "Service for evaluating transactions created from Transaction service. Uses KafkaJS to consume transaction-created events from `event-created` topic and produce transaction-evaluated events to `event-evaluated` topic.",
  "main": "index.js",
  "scripts": {
    "start:dev": "ts-node ./src/index.ts",
    "test": "jest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/backoff": "^2.5.5",
    "backoff": "^2.5.0",
    "common-tools": "^1.0.0",
    "dotenv": "^16.4.5",
    "kafka-node": "^5.0.0",
    "pino": "^8.19.0",
    "ts-node": "^10.9.2"
  },
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "ts-jest": "^29.1.2"
  },
  "directories": {
    "test": "tests"
  }
}
